name: Develop Workflow

on:
  push:
    branches: [ develop ]

env:
  REGISTRY: ghcr.io
  OWNER_REPO: namnextx
  IMAGE_NAME: ghcr.io/namnextx/${{ github.event.repository.name }}
  ENV_DEPLOY: dev

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  development-ci-cd:
    environment: Development Environment
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 1
          persist-credentials: true
          ref: develop
          path: main

      - name: Set up JDK 11
        if: success()
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: maven

      - name: Cache Maven packages
        uses: actions/cache@v1
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Package
        working-directory: main
        run: mvn package -DskipTests

      # Build and Push Docker image on Repository Github
      - name: Set up Docker Buildx
        if: ${{ success() }}
        uses: docker/setup-buildx-action@v1

      - name: Log in to the Container registry
        if: ${{ success() }}
        uses: docker/login-action@v1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v3.6.2
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=sha,enable=true,prefix=${{ env.ENV_DEPLOY }}-

      - name: Build and push Docker image
        if: ${{ success() }}
        uses: docker/build-push-action@v2
        with:
          context: main
          file: ./main/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: JAVA_OPTS=${{ secrets.DOCKER_JAVA_OPTIONS }}
      - name: Checkout k8s-helm-deployment Repo
        uses: actions/checkout@v2
        if: ${{ success() }}
        with:
          repository: namnextx/k8s-helm-deployment
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1
          persist-credentials: true
          ref: dev
          path: k8s-helm-deployment
      - name: Update Image Version in the related HelmChart values.yaml
        uses: fjogeleit/yaml-update-action@main
        with:
          workDir: k8s-helm-deployment
          repository: namnextx/k8s-helm-deployment
          token: ${{ secrets.GITHUB_TOKEN }}
          valueFile: 'servicesvalues/k8s-java-application-chart.values.yaml'
          propertyPath: 'k8s-helm.image.tag'
          value: ${{ steps.meta.outputs.version }}
          branch: develop
          createPR: false
          updateFile: true
          message: 'Update Image Version to ${{ steps.meta.outputs.version }} to ${{ github.event.repository.name }}'